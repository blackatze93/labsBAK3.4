vich_uploader:
    db_driver: orm # or mongodb or propel or phpcr
    mappings:
        documentos:
            uri_prefix: '%app.path.documentos%'
            upload_destination: '%kernel.root_dir%/../public/%app.path.documentos%'

        imagenes_elementos:
            uri_prefix: '%app.path.imagenes_elementos%'
            upload_destination: '%kernel.root_dir%/../public/%app.path.imagenes_elementos%'

    #mappings:
    #    products:
    #        uri_prefix: /images/products
    #        upload_destination: '%kernel.project_dir%/public/images/products'
